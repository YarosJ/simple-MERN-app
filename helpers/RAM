export default (file) =>
    setInterval(function () {
      const used = process.memoryUsage();
      for (let key in used) {
        console.log(file,`${key} ${Math.round(used[key] / 1024 / 1024 * 100) / 100} MB`);
      }
    }, 2000);